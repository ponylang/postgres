## Add package-level documentation

The `postgres` package now includes a package-level docstring that appears in generated API documentation. It covers connecting, SSL/TLS, query types (SimpleQuery, PreparedQuery, NamedPreparedQuery), working with results, named prepared statements, bulk loading with COPY IN, query cancellation, and authentication.
